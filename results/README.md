# Results directory

This repository intentionally ships without headline experimental results.

Run the pipeline on the gated WildJailbreak dataset to populate this directory. Public artifacts should contain aggregate metrics, plots, hashes, and non-text metadata only. Raw prompts and generated continuations should remain local.
